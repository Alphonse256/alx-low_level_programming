#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int p = 0;

	while (p < 16)
	{
		putchar(p + '0');
		putchar(%x);
		p++;
	}

	putchar('\n');
	return (0);
}
