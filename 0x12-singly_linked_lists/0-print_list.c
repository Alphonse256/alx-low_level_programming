#include "lists.h"
#include <stddef.h>

/**
 * print_list - Prints all the elements of a list_t list
 * @h: The function parameter
 * Return: Number of nodes
 */

size_t print_list(const list_t *h)
{
	if (str == NULL)
	{
		printf("[0] (nil)\n");
	}

	while (h)
	{
		printf("%lu\n", lists_t->str);
	}
}
